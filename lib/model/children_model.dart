class ChildrensModel {
  late String name;
  late String country;
  late bool status;
  // Model for children for better understanding of data flow in the app
  ChildrensModel({
    required this.name,
    required this.country,
    required this.status,
  });
}
